#Factorial of n == n! = 1*2*3....*(n-1)*n
def factorial_recursive(n) :
    if n ==1 or n ==0 :
        return 1
    return n * factorial_recursive(n-1)

n = int(input("Enter num to find its Factorial : "))
print(f"Factorial of {n} = {factorial_recursive(n)}")
